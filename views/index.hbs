<!-- ./views/index.hbs -->

<h1>Photos in Album</h1>
<a href="/">Back to Albums</a>
<div class="photo-grid">
  {{#each photos}}
    <div class="photo-item">
      <img
        src="/images/{{../albumUUID}}/{{this.original_filename}}"
        alt="{{this.original_filename}}"
      />
      <p>Score: {{this.score.overall}}</p>
    </div>
  {{/each}}
</div>
